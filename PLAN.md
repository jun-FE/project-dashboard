# 프로젝트 관리 대시보드 — 기획서 (PLAN.md)

> 이 문서는 Claude Code가 읽고 단계별로 구현하기 위한 기획서다.
> **중요: 이 문서를 읽은 뒤 바로 전체 코드를 작성하지 말 것.** 아래 "개발 로드맵"의 Phase 1부터 하나씩, 각 Phase가 끝나면 사용자 확인을 받고 다음으로 진행한다.

---

## 1. 프로젝트 목표

여러 개의 성격이 다른 개인 사업 프로젝트(쿠팡 체험단 리뷰, 워드프레스 블로그, 쇼츠 채널 등)를 **한 곳에서 진행상황을 파악**하기 위한 개인용 대시보드.

핵심 설계 원칙:

- **프로젝트마다 성격이 다르다.** 하나의 워크플로우로 통일하지 않는다. 메인은 공통 정보만, 상세는 프로젝트별 맞춤.
- **어떤 프로젝트가 새로 추가돼도 구조 변경 없이 수용**한다. (공통 컬럼 + 자유 필드 구조)
- **AI 팀원(Hermes)이 데이터를 직접 읽고 갱신**한다. 사람만 쓰는 UI가 아니라, AI가 API로 상태를 업데이트하는 것을 전제로 설계한다.
- 사용자는 React 개발자이지만 직접 코딩하지 않는다. 코드는 Claude Code가 작성한다.

---

## 2. 확정된 기술 스택 (변경 금지)

- **데이터**: Supabase (PostgreSQL). 테이블은 Supabase에서 관리하고, 프론트와 AI 모두 Supabase API로 접근.
- **프론트**: Vite + React + TypeScript + Tailwind CSS. 단일 SPA.
- **라우팅**: React Router (메인 `/`, 상세 `/project/:id`).
- **배포**: Vercel (무료 티어). 도메인 연결은 추후.
- **인증**: v1에서는 생략 (개인용). 추후 추가.

---

## 3. 화면 구성

### 3.1 메인 대시보드 (`/`)

모든 프로젝트가 **공통으로** 가진 정보만 카드 형태로 나열. (칸반 아님 — 프로젝트마다 워크플로우가 달라 카드 그리드가 적합)

각 카드에 표시:

- 프로젝트 이름
- 카테고리 뱃지 (개발 / 리서치 / 콘텐츠 / 커머스 등)
- 한 줄 상태 메시지
- 진행률 바 (0~100%)
- 이번 주 목표 달성 현황 (예: "리뷰 7/10") — 정기 목표가 설정된 프로젝트만
- 마지막 업데이트 시각 (상대 시간: "2시간 전")
- 활성/일시정지 상태 표시
- **오늘 한 일**: 오늘 작성된 `progress_logs`를 기준으로 "오늘 N건 · {가장 최근 로그 한 줄}" 형태 표시. 마우스 호버 시 오늘 로그 전체가 팝업 리스트로 표시. 오늘 기록이 없으면 "오늘 기록 없음"으로 표시(영역은 모든 카드에 유지). (스키마 변경 없음 — `log_date`가 오늘인 로그만 필터링)

상단 요약 영역:

- 진행 중 프로젝트 수
- 이번 주 전체 목표 달성률
- 최근 업데이트된 프로젝트

카드 클릭 → 해당 프로젝트 상세 페이지로 이동.

### 3.2 프로젝트 상세 페이지 (`/project/:id`)

**프로젝트 성격에 따라 다른 내용을 보여주는** 페이지. 공통 영역 + 프로젝트별 자유 영역으로 구성.

공통 영역:

- 프로젝트 이름, 설명, 카테고리, 상태
- 진행률 및 정기 목표 추적 (주간/월간: 목표 대비 현재, 예 "이번 주 10개 중 7개 완료")
- 진행 로그 타임라인 (최신순)

프로젝트별 자유 영역 (`custom_fields` 기반으로 렌더링):

- 쿠팡 리뷰 프로젝트 예: 이번 주 리뷰 목표/완료, 받은 상품 목록
- 쇼츠 프로젝트 예: 구독자 수, 이번 주 업로드 영상 수, 다음 업로드 예정
- 블로그 프로젝트 예: 이번 달 발행 글 수, 애드센스 수익, 트래픽

> 자유 영역은 정해진 키가 있으면 보기 좋게, 없으면 key-value로 단순 표시하는 방식으로 유연하게 렌더링한다.

---

## 4. 데이터 모델 (초안)

확장성의 핵심: **공통 컬럼은 고정, 프로젝트별로 다른 정보는 `custom_fields` (JSONB)에 담는다.** 새 프로젝트가 전혀 다른 성격이어도 테이블 구조를 바꾸지 않는다.

### 4.1 `projects`

| 컬럼 | 타입 | 설명 |
|------|------|------|
| id | uuid (PK) | 프로젝트 고유 ID |
| name | text | 프로젝트 이름 |
| description | text | 설명 |
| category | text | 카테고리 (개발/리서치/콘텐츠/커머스 등) |
| status | text | active / paused / done |
| progress | int | 진행률 0~100 |
| custom_fields | jsonb | 프로젝트별 자유 데이터 (구독자수, 수익 등) |
| created_at | timestamptz | 생성일 |
| updated_at | timestamptz | 마지막 갱신 (AI/사람이 업데이트할 때마다 갱신) |

### 4.2 `recurring_goals` (정기 반복 목표)

쿠팡 주 10개, 쇼츠 주 N개 같은 주간/월간 반복 목표 추적용.

| 컬럼 | 타입 | 설명 |
|------|------|------|
| id | uuid (PK) | |
| project_id | uuid (FK → projects.id) | 소속 프로젝트 |
| label | text | 목표 이름 (예: "주간 리뷰 작성") |
| period | text | weekly / monthly |
| target_count | int | 목표 개수 (예: 10) |
| current_count | int | 현재 달성 개수 (예: 7) |
| period_start | date | 현재 집계 주기 시작일 (주기 리셋 기준) |

### 4.3 `progress_logs` (진행 로그)

| 컬럼 | 타입 | 설명 |
|------|------|------|
| id | uuid (PK) | |
| project_id | uuid (FK → projects.id) | 소속 프로젝트 |
| log_date | timestamptz | 로그 시각 (ISO) |
| log_type | text | 작업 / 결정 / 이슈 |
| content | text | 내용 |

---

## 5. 확장성 설계 (어떤 프로젝트가 추가돼도 대응)

1. **공통 + 자유 분리**: 모든 프로젝트가 공유하는 필드만 컬럼으로 고정하고, 나머지는 `custom_fields` JSONB에 담는다. 새 프로젝트 추가 = 행 하나 추가일 뿐, 스키마 변경 없음.
2. **상세 페이지의 동적 렌더링**: 상세 페이지는 `custom_fields`를 순회하며 렌더링하므로, 새 종류의 데이터가 들어와도 자동으로 표시된다.
3. **카테고리는 자유 텍스트 + 추천값**: enum으로 못 박지 않아 새 분류가 생겨도 수용.

---

## 6. AI(Hermes)가 데이터를 읽고 갱신하는 흐름

이 대시보드는 사람만 쓰는 게 아니라 AI 팀원이 직접 갱신하는 것을 전제로 한다.

- **읽기**: Hermes가 Supabase REST API로 `projects`, `recurring_goals`, `progress_logs`를 조회해 "현재 어떤 프로젝트가 어디까지 진행됐는지" 파악.
- **쓰기**: 작업을 완료하면 Hermes가 해당 프로젝트의 `progress`, `custom_fields`, `updated_at`을 갱신하고 `progress_logs`에 한 줄 기록. 정기 목표를 하나 끝내면 `recurring_goals.current_count`를 +1.
- **구현 메모**: v1에서는 프론트와 동일하게 Supabase API 키로 접근. 추후 AI 전용 권한 분리 가능.

> 이 흐름이 동작하려면 데이터 구조가 단순하고 예측 가능해야 한다. 그래서 테이블을 최소 3개로 단순하게 유지한다.

---

## 7. 개발 로드맵 (Phase별 — 하나씩 진행, 각 Phase 후 사용자 확인)

### Phase 0 — 프로젝트 초기화
- Vite + React + TS + Tailwind 프로젝트 생성, 폴더 구조 잡기, 로컬에서 빈 화면 뜨는 것까지 확인.
- **확인 받고 다음으로.**

### Phase 1 — Supabase 스키마
- 위 3개 테이블(`projects`, `recurring_goals`, `progress_logs`) 생성 SQL 작성.
- 테스트용 더미 데이터 2~3개 삽입 (쿠팡 리뷰, 쇼츠, 블로그 예시).
- **사용자가 Supabase에 적용하고 확인 받고 다음으로.**

### Phase 2 — 메인 대시보드 (읽기 전용)
- Supabase 연결, `projects` 불러와서 카드 그리드로 표시.
- 상단 요약 영역.
- **확인 받고 다음으로.**

### Phase 3 — 프로젝트 상세 페이지
- 라우팅 추가, 카드 클릭 → 상세 이동.
- 공통 영역 + `custom_fields` 동적 렌더링 + 정기 목표 추적 표시 + 진행 로그 타임라인.
- **확인 받고 다음으로.**

### Phase 4 — 배포
- Vercel 배포, 환경변수(Supabase URL/키) 설정.
- **확인 받고 다음으로.**

### Phase 5 (추후) — AI 갱신/쓰기, 인증, 도메인 연결
- Hermes가 쓰기 가능하도록 정리, 필요 시 인증·도메인.

---

## 8. 작업 시 규칙 (Claude Code용)

- 한 번에 한 Phase씩. Phase 끝나면 멈추고 사용자 확인.
- Supabase 연결 정보(URL, anon key)는 코드에 하드코딩하지 말고 `.env`(`VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY`)로 관리.
- 모르는 부분(특히 Supabase 키, 배포 설정)은 추측하지 말고 사용자에게 먼저 질문.
- 사용자는 React 개발자이므로 코드 구조/선택은 간단히 근거를 덧붙여 설명.
